#include<stdio.h>
int main()
{
    int i,number;
    printf("enter any number=");
    scanf("%d",&number);
    printf ("divisor of number is="); 
    for(i=1;i<=number;i++)
    {
        if(number%i==0)
        {
            printf("%d", i);
        }
    }
}